To run the BLE version of this application, replace the "C:\Users\...\BLUETOOTH" with your directory path (where the files are located) and type the following into the Command Prompt:

---
cd "C:\Users\...\BLUETOOTH"
python BLE_app.py
---


Structure of the bluetooth version of the code database:
